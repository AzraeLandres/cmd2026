PORT=3000
FOOTBALL_DATA_API_KEY=
GROQ_API_KEY=
TOKEN_SECRET=change_this_secret
DATABASE_URL=postgresql://user:password@localhost:5432/cdm2026

# Rôle restreint (SELECT/INSERT/UPDATE/DELETE) utilisé par l'app au quotidien.
# Créé/mis à jour par `npm run db:migrate` (voir README). Optionnel : sans ces
# variables, l'app retombe sur DATABASE_URL (rôle propriétaire).
PG_APP_USER=cdm_app
PG_APP_PASSWORD=
